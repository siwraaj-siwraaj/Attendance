name: AI Issue to PR

on:
  issues:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  issue-automation-test:
    runs-on: ubuntu-latest
    steps:
      - name: Ask AI to read the issue
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          response=$(curl -sS https://api.openai.com/v1/responses \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "$(jq -n \
              --arg title "$ISSUE_TITLE" \
              --arg body "$ISSUE_BODY" \
              '{
                model: "gpt-5.6-luna",
                input: ("Read this GitHub issue. Reply with a one-sentence summary. Title: " + $title + " Body: " + $body)
              }')")

          echo "AI response:"
          echo "$response" | jq -r '.output[0].content[0].text // .error.message // "No response text returned."'
