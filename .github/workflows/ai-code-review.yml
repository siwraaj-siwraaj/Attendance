name: "AI Pull Request Reviewer"

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Validate pull request workflow
        run: echo "AI review integration is temporarily disabled; repository build checks remain authoritative."
